spring:
  jpa:
    hibernate:
      format_sql: true
      use_sql_comments: true
    show-sql: true
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true
  defer-datasource-initialization: true