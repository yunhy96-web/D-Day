// 광고 배너 컴포넌트 (플레이스홀더)
// TODO: 나중에 react-native-google-mobile-ads 또는 expo-ads-admob을 사용하려면
// Development Build (EAS Build)가 필요합니다.

interface AdBannerProps {
  style?: object;
}

export function AdBanner(_props: AdBannerProps) {
  // 개발 환경에서는 빈 컴포넌트 반환
  // 프로덕션 빌드에서 광고를 활성화하려면 Development Build를 사용하세요
  return null;
}
