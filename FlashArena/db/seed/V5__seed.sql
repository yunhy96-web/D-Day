-- =============================================================
-- FlashArena :: V5 - 시드 데이터
-- -------------------------------------------------------------
-- 시뮬레이션 대상 상품 1건. 재고는 시뮬레이터에서 리셋 API로
-- 다시 채울 수 있게 설계하지만, 초기값으로 100개를 넣어둔다.
-- (auth 공용 계정은 BCrypt 인코딩 때문에 Phase 2 앱에서 시딩)
-- =============================================================

INSERT INTO "order".product (name, price, quantity)
SELECT '한정판 굿즈 (Flash Sale)', 19900, 100
WHERE NOT EXISTS (
    SELECT 1 FROM "order".product WHERE name = '한정판 굿즈 (Flash Sale)'
);
